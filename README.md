# Flask DevOps Lab B
## Usage
```
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```
- `/api/health` – Returns the application's health status.
- `/api/config` – Displays the current configuration.
- `/api/report` – Returns a report generated by the application.
